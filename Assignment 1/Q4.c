// write a program to check whether a given character is a vowel or a constant

void main()
{
	char A = 'z';
	if ((A == 'a' ) || (A == 'e' ) || (A == 'i') || (A == 'o') || (A == 'u')) 
	{
		printf("is vowel");
	}
	else
     {
		printf("is constant");
     }
}